<tabCom></tabCom>
<view class="main">
    <scroll-view scroll-y class="mainLeft">
        <view class="leftItem" wx:for="{{leftNav}}" wx:key="index">
            <view class="inside">{{item.cat_name}}</view>
        </view>

    </scroll-view>
    <scroll-view scroll-y class="mainRight">
        <view class="firstItem" wx-for="{{rightChildren[currentIndex]}}" wx-key="index">
            <view class="titel">{{item.cat_name}}</view>
            <view class="main">
                <view class="secondItem">

                </view>
            </view>
        </view>
    </scroll-view>
</view>